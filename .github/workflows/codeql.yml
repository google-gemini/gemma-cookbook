# Replaces the original CodeQL workflow to avoid conflicts with Default Setup.
# This dummy workflow ensures the check passes if it is required.

name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '27 20 * * 5'

jobs:
  analyze:
    name: Analyze (Dummy)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - name: Dummy Pass
        run: echo "CodeQL Advanced is disabled in favor of Default Setup. This is a placeholder to ensure checks pass."
